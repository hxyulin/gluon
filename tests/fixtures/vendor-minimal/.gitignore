# Gluon build artefacts.
/build/

# Vendored crate sources — regenerated by `gluon vendor`.
# Commit `gluon.lock` and the scratch `build/vendor-workspace/Cargo.lock`
# (gluon writes both) instead. Offline builds from a fresh clone can
# remove this entry to commit the vendored sources directly.
/vendor/
